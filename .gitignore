# stellar/soroban/Rust output
target
.soroban

# project identity and alias files will be in .config/stellar
.config

# test snapshot folders from sample contracts
test_snapshots

# build output
dist
dist-ssr

# dependencies
node_modules/

# logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# environment variables
.env
.env.*
!.env.example
!**/.env.example

# Stellar CLI identities / secrets (never commit)
identity/
*.toml.bak
**/identity/**

# easy way to create temporary local-only files and folders
*.local.*

# macOS-specific files
.DS_Store

# generated contract bindings + clients
app-lib/clients/*
!app-lib/clients/.gitkeep

# Agent/editor scratch
.claude/
.cursor/

# TypeScript incremental build info
*.tsbuildinfo

# Scaffold CI not used for hackathon deploy
.github/

